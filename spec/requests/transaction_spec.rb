require "rails_helper"
require "spec_helper"
RSpec.describe "Transactions", type: :request do
  # let(:user) { FactoryBot.create(:user, email: "Shariff@email.com", password: "23456789", password_confirmation: "23456789", name: "Shariff") }
  # let(:transaction) { FactoryBot.create(user_id: user.id, input_amount: 1000, input_currency: "KES", output_currency: "USD") }
  # describe "GET /index" do

  #   # let(:admin) { users(:admin) }
  #   # get :index,  headers: { "Authorization" => AuthenticateUser.call(user.email, user.password).result }
  #   { get "/api/transactions", headers: { "Authorization" => AuthenticateUser.call(user.email, user.password).result } }
  #   expect(response).to be_successful
  #   expect(response_json.size).to eq 1
  #   expect(response_json.first["id"]).to eq todo.id
  # end
end
