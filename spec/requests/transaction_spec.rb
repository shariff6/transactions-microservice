require 'rails_helper'
require 'spec_helper'
RSpec.describe "Transactions", type: :request do

  # let(:user) { FactoryBot.create(:user, email: "Shariff.email.com", password: "23456789", password_confirmation: "23456789", name: "Shariff" ) }
  # describe "GET /index" do

  #   # let(:admin) { users(:admin) }

  #   before { get '/api/transaction', headers: { 'Authorization' => Authentication.call(user.id) } }

  #   it 'is expected to assign transaction instance variable' do
  #     expect(assigns[:transactions]).to eq(Transaction.all)
  #   end
end
end

